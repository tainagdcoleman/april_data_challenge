# Copy to .env and fill in.

# --- Chat model (NRP ellm) ---------------------------------------------------
# https://ellm.nrp-nautilus.io
NRP_API_KEY=

# --- Vector database ---------------------------------------------------------
# Uses ChromaDB (file-based, stored under ./chroma_db/). No extra credentials.

# --- Optional: OpenAI (only if you add openai/* entries to models.yaml) ------
# OPENAI_API_KEY=
